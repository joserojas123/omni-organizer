# Base URL of the Omni organize API. When unset, the app runs in local-only mode
# (áreas, proyectos y objetivos persisten en localStorage). Set this to enable server persistence.
NEXT_PUBLIC_API_URL="http://localhost:4000/api"
